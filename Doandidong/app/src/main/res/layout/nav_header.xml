<?xml version="1.0" encoding="utf-8"?>
<!-- File: nav_header.xml -->
<!-- Mô tả: Layout cho phần header của Navigation Drawer. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="180dp"
    android:background="?attr/colorPrimaryDark"
    android:gravity="bottom"
    android:orientation="vertical"
    android:padding="16dp"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <!-- Avatar của người dùng (ảnh tĩnh) -->
    <ImageView
        android:id="@+id/nav_header_avatar"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:src="@mipmap/ic_launcher_round"
        android:contentDescription="Avatar người dùng" />

    <!-- Tên của người dùng, sẽ được cập nhật từ SharedPreferences -->
    <TextView
        android:id="@+id/nav_header_user_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="8dp"
        android:text="User Name"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"
        android:textStyle="bold"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Chào mừng trở lại!"/>

</LinearLayout>
