<?xml version="1.0" encoding="utf-8"?>
<!-- File: layout_man_hinh_them_danh_muc.xml -->
<!-- Mô tả: Layout cho màn hình thêm danh mục mới. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F4F6F8">

    <!-- Toolbar chung -->
    <include android:id="@+id/layoutHeader" layout="@layout/layout_toolbar_con"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- Ô nhập liệu cho tên danh mục -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tên danh mục"
            android:textStyle="bold"
            android:textSize="16sp"/>
        <EditText
            android:id="@+id/edtCategoryName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="48dp"
            android:hint="Ví dụ: Đồ gia dụng"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="24dp"/>

        <!-- Nút Lưu -->
        <Button
            android:id="@+id/btnLuuDanhMuc"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Lưu"
            android:textSize="18sp"/>

    </LinearLayout>
</LinearLayout>
