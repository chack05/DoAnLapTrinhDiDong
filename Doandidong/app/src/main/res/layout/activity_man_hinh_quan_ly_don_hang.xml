<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F0F4F8">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Toolbar tùy chỉnh đã được đồng bộ -->
        <include
            android:id="@+id/layoutHeader"
            layout="@layout/layout_toolbar_con" />

        <!-- Ô tìm kiếm đơn hàng -->
        <EditText
            android:id="@+id/edtTimKiemDon"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="16dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="8dp"
            android:hint="Tìm theo tên khách hoặc mã đơn..."
            android:padding="10dp"
            android:drawableStart="@android:drawable/ic_menu_search"
            android:drawablePadding="8dp"
            android:background="@drawable/spinner_background"
            android:minHeight="48dp" />

        <!-- Spinner lọc theo sản phẩm -->
        <Spinner
            android:id="@+id/spinnerLocTheoSanPham"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="16dp"
            android:layout_marginBottom="8dp"
            android:background="@drawable/spinner_background"
            android:minHeight="48dp" />

        <!-- Phần sub-header hiển thị tổng số lượng -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="16dp"
            android:paddingBottom="12dp"
            android:background="@android:color/white"
            android:elevation="2dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Danh sách đơn hàng\nNHÓM 4 / PHAN THANH TAI"
                android:textColor="#3F51B5"
                android:textSize="18sp"
                android:textStyle="bold"/>

            <TextView
                android:id="@+id/tvTongSoLuongDon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Tổng số lượng: 0"
                android:textSize="14sp"
                android:layout_marginTop="4dp"/>
        </LinearLayout>

        <!-- RecyclerView hiển thị danh sách đơn hàng -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerViewDonHang"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="8dp"
            android:clipToPadding="false"/>

    </LinearLayout>

    <!-- Floating Action Button để thêm đơn hàng mới -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabThemDonHang"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:src="@android:drawable/ic_input_add"
        app:backgroundTint="?attr/colorPrimary"
        app:tint="@android:color/white"
        android:contentDescription="Thêm đơn hàng mới" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
